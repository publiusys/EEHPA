```
sudo apt install python3-pip
pip install notebook
pip install numpy
pip install pandas
pip install matplotlib

export PATH=$PATH:/users/hand32/.local/bin

jupyter-notebook --ServerApp.allow_origin='*' --ServerApp.ip='*
```